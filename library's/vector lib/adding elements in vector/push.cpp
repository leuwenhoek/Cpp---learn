#include <iostream>
#include <vector>

using namespace std;

int main(){

    vector<int> num;            //declaring a integer vector
    num.push_back(10);          //adding an element in the vector(10).
    num.push_back(20);          //adding an element in the vector(20).
    num.push_back(30);          //adding an element in the vector(30).
    num.push_back(40);          //adding an element in the vector(40).
}