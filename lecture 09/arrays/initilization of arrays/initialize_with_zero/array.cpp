#include <iostream>

using namespace std;

int main(){

    int zero_array[5] = {};             //setting all values to zero, we can also write it as '{0}'

    for (int i = 0; i < 5; i++){
        
        cout<<zero_array[i]<<" ";
    }
     
}