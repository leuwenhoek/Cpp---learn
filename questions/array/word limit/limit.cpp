//not completed yet

#include <iostream>
#include <vector>

using namespace std;

int main(){

    int limit;
        cout<<"Enter the word limit that you want : ";
        cin>>limit;

        for (int i = 0; i <= limit; i++){
            vector<string>limited_words(limit);
            cin>>limited_words;
        }
      
}